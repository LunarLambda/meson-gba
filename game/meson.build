message(
  'To get started with meson-gba, copy the files from a template into\n' +
  'the "game" directory, then recompile using ninja -C <your build directory>.\n' +
  'It\'s recommended you start with the template "first", unless\n' +
  'you already have a library you want to use.\n\n')
